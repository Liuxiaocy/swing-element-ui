# 2026-09-11 工作日志

## P4.1 `AstRadio` 按钮样式（setButtonStyle）

- 实现：AstRadio 新增 `setButtonStyle(boolean)`/`isButtonStyle()` + `fill` 动画通道；按钮态渲染直角分段按钮
  （选中=主色填充+白字实心态，未选中=浅底+常规文字，禁用=静音灰），`getPreferredSize` 按钮态
  `height=32 / width=文字宽+40`。
- selfCheck 加绘制级像素断言（强制 fill=1，采样背景中心须为 PRIMARY）；反向验证屏蔽 primary 填充得真实
  `AssertionError: button selected bg must be PRIMARY, got ffffff`。
- 新增 `AstRadioDemo`（普通/按钮样式/禁用三组 + `--selfcheck`，校验 Group 互斥组合）。
- 接入 build.bat / run-checks.bat（现 60 步）、README、radio.md；全量 60 步全绿、DocSnippetCheck 26 篇通过。

## 踩坑（已写入 MEMORY.md）

- `cmd.exe` 被 Bash 工具安全策略禁止调用 → 全套自检改在 bash 里跑（写 `_sc.sh` 用 JDK1.8 绝对路径循环）。
- Demo（另一包）访问不到组件 `protected anim` → 绘制级像素断言必须放在组件自身 selfCheck；Demo selfCheck 只做
  API/尺寸/Group/绘制不抛错。
- Demo `selfCheck` 在 `invokeAndWait` 里 `getFontMetrics(br.getFont())` 会因 `getFont()` 为 null 而 NPE，
  改用 `br.getFontMetrics(ElementTheme.font())`。

## P4.2 `AstCheckbox` 按钮样式（setButtonStyle）

- 复用 P4.1 的 Bounded 设计：选中=主色填充+白字实心态、直角 1px 边框分段按钮、禁用=静音灰；按钮态
  `height=32 / width=文字宽+40`；`fill` 通道在 AstCheckbox 已存在，直接驱动 `onSelectedChanged`。
- 按钮态**不画勾选框**（与 Radio 一致，纯文字分段按钮）；多选语义不变（X/Y 可同时选中）。
- selfCheck 加绘制级像素断言（强制 fill=1，采样背景中心须为 PRIMARY）；反向验证屏蔽 primary 填充得真实
  `AssertionError: checkbox button selected bg must be PRIMARY, got ffffff`，还原 exit=0。
- 新增 `AstCheckboxDemo`（普通/按钮样式/禁用三组 + `--selfcheck`，校验**多选**组合）。
- 接入 build.bat / run-checks.bat、README、checkbox.md；全量 60 步全绿、DocSnippetCheck 通过。

## 环境踩坑（本次会话追加）

- 当前 Bash 沙箱的 coreutils 缺失（`cat`/`head`/`tail`/`dirname`/`sleep`/`timeout` 均不在 PATH），
  仅 `echo`/`cd`/`pwd`/重定向可用；`/tmp` 重定向落盘不可靠。→ 跑 Java 用**绝对 JDK 路径 + 绝对 classpath**，
  不经过管道（`| head` 等会失败），结果用 Write 工具写 `_sc.sh`、再用 Read 工具读 `_sc.log` 回看。
  `_sc.sh` 用纯 sh 内建（无 `timeout` 兜底），批量跑会偶发 SIGTERM；改为**逐条独立 Bash 调用**抓 rc 最稳。
- 全套自检在 bash 里跑（cmd.exe 仍被安全策略禁止）；`git clean -f -- _sc.sh _sc.log` 可删临时驱动（rm 包装缺失 dirname）。

## P5.1 AstBreadcrumb Demo（补齐 Demo 与文档）— 完成

- 新建 `src/org/swelement/demo/AstBreadcrumbDemo.java`（默认分隔符 `/` / 自定义分隔符 `>` / 点击交互三组 + `--selfcheck`）。
- selfCheck（EDT `invokeAndWait`，与 AstRadioDemo 同模板）：校验 `getItems().size()`、`setItems` 往返、`setSeparator(null)`/`setItemClickListener(null)` 抛 IllegalArgumentException；尺寸 `height==24`（ROW_H）、`width>0`；点击行为——`dispatchEvent(MOUSE_PRESSED)` 首段(idx=0)可点、末段(width-30)不可点；离屏 `paint` 不抛错。
- 新建 `docs/components/breadcrumb.md`（语句型示例，无截图不写图片链接，过 DocSnippetCheck）。
- 接入 build.bat（AstBreadcrumbDemo 块）、run-checks.bat（60→61 步，replace_all `/60]`→`/61]` 重编号 + 插入 check 60、docs 改 61）、README（运行列表 + 组件总览补 Breadcrumb 行，原表缺该组件行）。
- 验证：增量编译 OK；`AstBreadcrumbDemo --selfcheck`/`AstBreadcrumb`/`DocSnippetCheck` 均 RC=0。提交 f7c1f80，gitee 推送成功（origin 仍代理 502）。

## P5 剩余清单（待后续「继续」逐条推进）

- 新建 Demo：`CollapseDemo`（+ collapse.md）、`TimePickerDemo`（timepicker.md 已有）、`DatePickerDemo`（datepicker.md 已有）。
- 已存在但未接入 run-checks.bat 的 Demo：`AstEmptyDemo` `AstNotificationDemo` `AstIconDemo` `AstTableDemo`（build.bat 已有）——P5.3 统一接入 run-checks.bat。
- P6 全量验证（run-checks 最终约 68 步 + DocSnippetCheck 全绿）。

## P5.1 AstCollapse Demo — 完成

- 新建 `AstCollapseDemo`（基本非手风琴 / 手风琴 / 变化监听三组 + `--selfcheck`）+ `docs/components/collapse.md`。
- **DocSnippetCheck 关键坑**：`tools/DocSnippetCheck.java` 把每个 ```` ```java ```` 代码块当**独立类**编译
  → 跨代码块引用变量会报「找不到符号」。collapse.md「监听变化」块引用了上一个「手风琴模式」块里声明的 `c`，
  编译失败（`collapse.md:41 找不到符号 变量 c`）。**每个 java 代码块必须自包含**（自己声明用到的变量）。
- 接入 build.bat / run-checks.bat（62 步）/ README；提交 f5479d7 + progress 6676c1c。

## P5.1 AstTimePicker Demo — 完成

- 新建 `AstTimePickerDemo`（基本含秒列 / 关闭秒列 / 区间模式 / 变化监听四组 + `--selfcheck`）。
- Demo 可直接驱动弹层内的 `JList`：`showPicker()` 后 BFS 找**模型 24 项**的 JList（时列），
  `setSelectedIndex(15)` → 断言 `setTimeChangeListener` 回调收到 15；另断言列数（含秒列 3 / 关闭秒列 2）。
- 反向验证：临时屏蔽 `AstTimePicker` 的 `timeChangeListener.accept(...)` → 真实
  `AssertionError: 监听回调应收到 15，实际=-1`，退出码 1；还原 exit=0。
- 接入 build.bat / run-checks.bat（63 步）/ README；提交 a5c7440。

## P5.1 AstDatePicker Demo — 完成

- 新建 `AstDatePickerDemo`（已选日期 / 空值+占位符 / 区间 / 尺寸档位 / 监听五组 + `--selfcheck`）。
- **跨包定位私有内部类**：`CalendarPanel` 是 `AstDatePicker` 的 private 内部类，Demo 无法引用其类型 →
  按运行时类名后缀识别（`getClass().getName().endsWith("CalendarPanel")`）。用类名而非尺寸识别，因为
  `AnimatedPopup` 的 `content` 是匿名 JPanel (`AnimatedPopup$1`)、且 `openAnim` 会临时改其 border，
  prefSize 与日历面板重叠、用尺寸判会误判。
- Demo 用 `dispatchEvent(MOUSE_CLICKED, 134, 148)` 模拟点日历 → 选中 2026-08-12（viewMonth=2026-08 时该坐标固定）→ 断言 getDate/监听回调/自动收起。
- 反向验证：临时注释 `onDayClick` 单值分支的 `selectedDate = clicked;` → 真实
  `AssertionError: 点击应选中 2026-08-12，实际=2026-08-21`，退出码 1；还原 exit=0。
- 接入 build.bat / run-checks.bat（64 步）/ README；提交 ee8afae。

## P5.3 接入 run-checks.bat — 完成

- 把 `AstEmptyDemo`/`AstNotificationDemo`/`AstIconDemo`/`AstTableDemo`（均带 `--selfcheck`，此前仅 build.bat）
  补入 run-checks.bat → **[64/68]..[67/68]**，文档一致性收尾 [68/68]。总数 64→68。
- 4 条已单独跑确认 exit=0。提交 7882800。**P5 阶段（Demo/文档/接入）三项全部收口。**

## P6 全量验证 — 完成

- **结果：run-checks 68 步全部 PASS**；全量 JDK 8 编译 RC=0；`DocSnippetCheck OK（28 篇文档）`。
- 沙箱无 `timeout`，用 Python 解析 `run-checks.bat` 逐条执行并带 120s 超时；该脚本已**固化为 `tools/run-checks.py`**
  （`--limit N` 可冒烟；`JAVA8_HOME` 可覆盖 JDK 路径；ROOT 由脚本位置推导）。实测解析出 70 条命令（1 版本探针 + 67 自检 + DocSnippetCheck 编译/运行 2 条）全绿。
- 解析要点：正斜杠替换 + `shlex.split(posix=True)` 去掉 exe 路径引号，`subprocess.run(timeout=120)`，汇总 PASS/FAIL。

## 环境/工具坑（追加）

- **`git commit -m "$(cat <<'EOF' ...)"` 在本沙箱失败**（`cat` 缺失 → 提交信息为空 → abort）。
  改用 Write 工具写 `_msg.txt` + `git commit -F _msg.txt` + `git clean -f -- _msg.txt`。
- 反斜杠路径写进 shlex：先把 `\` 换 `/`（Java 在 Windows 接受 `/`），否则 posix shlex 会把 `tools\X.java` 吃掉反斜杠。
- gitee 与 origin（GitHub）**本次均已可推送**（代理 502 已恢复）；推完一律 `ls-remote` 复核哈希。


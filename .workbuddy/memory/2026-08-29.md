# 2026-08-29

## P4-A 修复批次（提交 fdeacfe，5 组件 + 2 demo，+305/-77）

用户本轮范围：先修全部 5 个 Bug。澄清："自动补全"=补全组件功能/展示类型，**非新建 Autocomplete 组件**；表格类型增强（固定表头/列、多级表头等）归入后续批次。

根因与修复：
- **AstDrawer 首次打开展开面板不显示**：GlassPane 新装后 body 未 validate。openNew 在 card.placeIn 后加 `gp.validate()`；resize 处理器加 `card.validate()`。selfCheck 新增 body 标签尺寸断言。
- **AstAlert 高度随父变化 + 文字过长**：高度改内容派生固定值（重写 `getMaximumSize` 防 BoxLayout 拉伸）；描述 wrap ≤2 行 + 省略号截断；标题单行省略号。新增 `wrapLines`/`truncate`/`computeFixedH`。
- **AstCalendar 翻月按钮无反应**：`drawNavButton` 仅绘制无点击。补 `navAt` hit-test + `mousePressed` 翻月/翻年；新增 `‹‹`/`››` 翻年按钮；上月/下月空白格绘制灰色补位日期，点击跳月并选中。新增 `prevYear`/`nextYear`/`daysInMonth`/`displayedDayForCell`。
- **AstDialog 关闭时页头/按钮最后消失**：`DialogCardPanel.paintComponent` 背景改全不透明；新增 `paint()` 用 `AlphaComposite(cardAlpha)` 包裹整卡统一淡出。
- **AstMessage 弹在屏幕外/非最前**：定位改用 owner 所在屏幕（`screenOf`），`lp.moveToFront` 置顶；AstPopupDemo 最大化窗口。

demo：AlertDemo 重写（AlertX 子类关闭后移除自身）；AstPopupDemo 补 Drawer/Calendar 段落 + 最大化。

验证：`javac --release 8`（本机 JDK 26）全量通过；AstDrawer/AstAlert/AstCalendar/AstDialog/AstMessage + 回归 AstMessageBox/AstDatePicker 共 7 个 self-check 全绿。

待办：P4-A 已推双远程（4413122..fdeacfe）。后续批次：P4-C 表格类型增强 / P4-D AstIcon 重构 / P4-E 卡片模板 / P4-F 尺寸·标识·Loading·穿梭框，待排期。

## P4-C 表格类型增强（设计 spec 已批准 27c6b45）

用户批准「C1→C9 全做」（固定表头+滚动 / 固定列 / 多级表头 / 单选多选 / 排序 / 筛选 / 展开行 / 合计行 / 合并+状态）。

设计要点（spec 路径 `docs/superpowers/specs/2026-08-29-p4-c-table-design.md`）：
- AstTable 由单 JComponent 重构为容器：HeaderView(吸顶) + BodyView(自管视口 scrollX/scrollY) + FooterView(合计)。
- 新增 `AstTableColumn`（title/width/align/sortable/fixed/children），旧 `Column` 保留为薄子类向后兼容。
- 新增 `AstTableModel`：原始行 + 视图索引(排序/筛选) + 选择 + 展开集 + spanMap + 合计聚合；原始 API 全保留。
- 冻结列：BodyView/HeaderView 用 clip 在左右固定 X 区重绘覆盖（C2/C3 先打地基）。
- 每批独立 TDD + 扩 selfCheck + 新 `AstTableDemo` 全展示 + 独立提交。
- 风险：冻结列+多级表头+横滚 clip 叠加最复杂；合并放 C9 最后。
- 流程进度：brainstorming(spec)✅ → 用户批准 ✅ → writing-plans✅(ab8bc18) → executing(Inline) 自 C1 起。

### P4-C 提交 ledger
- **C1** 重构为列模型+数据视图+分层渲染/固定表头+纵向滚动 — edebc17（self-check 全绿）
- **C2** 横向滚动+左/右冻结列(clip 重绘) — 9f288ca
- **C3** 多级表头(children 跨列合并) — 49a3b0c
- **C4** 单选/多选(选择列复选框) — 08b0029
  - 实现：AstTableModel 加 SelectionMode/SINGLE·MULTIPLE、selectedViewRows、toggleSelectedViewRow(单选置位/多选增删)、toggleSelectAll、getSelectedViewRows；AstTable 加 SELECTION_SINGLE/MULTIPLE 常量 + setSelectionMode 转发；多选模式 BodyView 首列画行复选框(冻结左 SELECT_COL_W=44)、HeaderView 全选复选框、点击行/复选框触发；leafXOnScreen/leafNaturalX 含 selectColW 偏移，maxScrollX 含选择列。
  - 注意：公开 row 索引统一为「视图行」语义（setSelectedRow/getSelectedRow 对称），无排序/筛选时等同于数据行，向后兼容旧 self-check。
- **C5** 排序(升/降/无 + 表头三角) — 160f8b0
  - `AstTableModel.SortDir{ASC,DESC,NONE}` + `sort(leafCol,dir)`；`compareValues` 数值按 Number、其余按 String。
- **C6** 筛选(列文本过滤 + 漏斗弹窗) — 8c43fa7
  - `filter(Predicate)`/`clearFilter()`/`getFilterActive()` + `AstTableColumn.filterable` 新字段（新增 7 参构造器，旧构造器默认 false）；表头最右画漏斗，点击弹 `JPopupMenu+JTextField` 内联输入。
  - 关键重构：`rebuildView()` 统一 view = 先筛选(raw) 再排序，addRow/setRows/sort/filter 均走它；故 C5 排序与 C6 筛选可叠加。
- **C7** 展开行 — 611564e
  - `expanded`(raw 行集)/`toggleExpanded`/`isExpandedView`/`expandedCount()`；`EXPAND_H=80`。
  - BodyView 改**槽位遍历**布局（数据行 + 展开块），`maxScrollY/scrollToRow/lastViewRowVisible/viewRowAtPoint` 全部按累计高度走，不再假设均匀行高；展开区返回 -1 不可选。双击行切换展开。
  - 公开 `getRowCount()` = 数据行 + 展开行（C7 自检测 +1）；模型内部选择边界仍用 `viewRowCount()`(数据行)。
- **C8** 表尾合计行 — d2fa3eb
  - `Aggregator{Object apply(List<Object[]> rows,int leafCol)}` + 内置 `SUM/AVG/COUNT`；合计基于**当前筛选后的视图行**，筛选变则重算；FooterView 有合计才可见，首列显示「合计」标签。
- **C9** 合并行/列 + 带状态表格 — 4abbdbe
  - `setSpan/getSpan`(raw 行键)、`Status{DEFAULT,SUCCESS,WARNING,DANGER,INFO}`、`setRowStatus`。
  - **BodyView 改两遍绘制**：Pass A 铺整行背景（选中/状态/斑马/hover），Pass B 按左/中/右冻结带 clip 画内容。原因：合并单元格跨行时，单行一遍绘制会被后画的下一行背景覆盖；且 clip 高度需按 `rowSpanRows` 放宽否则被截断。
  - 状态色用 `lerp(主题色, WHITE, 0.85)` 浅色底 + TEXT_MAIN，过 WCAG 断言。
- **C10** AstTableDemo + build.bat 注册 — 4940eaa
  - 10 个标签页（C1~C9 + 综合）；JTabbedPane；`--selfcheck` 参数离屏构建并绘制全部标签页。
  - build.bat SOURCES 增 `AstTableColumn.java AstTableModel.java AstTableDemo.java`；自检段增 AstTableDemo。
  - 全量 34 个 self-check 全绿（33 组件 + AstTableDemo）。
- 沙箱坑：`javac`/`java` 命令需 `dangerouslyDisableSandbox`；编译须全量 `src/org/swelement/core|ui|demo`（AstTable 依赖 core 包）；`cmd.exe` 无法从 PowerShell 工具调用，build.bat 需逐条 `java -ea -cp out ...` 在 bash 跑。
- Java 8 坑：内部类（非 static）不能有 `static` 方法；`BiFunction` 需 3 个类型参数（只要入参+出参时用 `Function`）。

### P4-C 收尾（已合并 main 并推双远程）
- 用户选择「合并 main 并推双远程」。main `ab8bc18` 是 p4-c 的祖先 → 直接快进。
- **踩坑**：`git checkout main` 被沙箱中断，留下 stale `index.lock` 且工作树被删掉大量源码（含 `src/org/swelement/demo/` 与 `AstTable*.java`）。因全部 10 个批次都已提交，用 `rm -f .git/index.lock` + `git checkout HEAD -- .` 完整恢复；`rm -rf out` 后全量重编译 + 34 个 self-check 复验 ALL_PASS。
- **规避方式（重要）**：改用 `git branch -f main p4-c` 完成快进合并 —— 完全不触碰工作树/索引，无 lock 风险。今后在本项目快进合并优先用这条。
- 推送：`GCM_INTERACTIVE=never GIT_TERMINAL_PROMPT=0 git -c credential.helper=wincred -c http.sslVerify=false push <gitee|origin> main`，两端均 `fdeacfe..4940eaa`，`ls-remote` 复核一致。
- 本地仍停在 `p4-c` 分支（工作树内容 = 4940eaa），未切回 main，避免再触发 checkout 问题。

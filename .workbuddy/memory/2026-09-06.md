# 2026-09-06

## AstInput 建议浮层收尾健壮性

- `refreshSuggestions()` 增加双层 `try/catch(Throwable)` + 顶部 `isDisplayable()` 早返回，覆盖 dispose 后 EDT 上偶发的 stale invokeLater（不会让 EDT 线程挂掉）
- 焦点监听 `focusGained` 用 `SwingUtilities.invokeLater` 异步分发，外层再包一层 `try/catch`，与 `refreshSuggestions` 内部互为兜底
- 编译单文件：`javac -encoding UTF-8 -cp out -d out src/.../AstInput.java`
- 自检通过：`AstInput self-check OK`（EXIT=0，无 AWT-EventQueue NPE）

### 调试痕迹

- 一开始 NPE 反复出现且 stack trace 行号与源码对不上（`AstInput.java:335` 指向 `new AnimatedPopup()` 而非 `rebuildSuggestionContent`），最终定位是 `out/production/swing-element-ui/...` 这条 Maven 残留 classpath 让 `javap` 与运行时看到不同版本的字节码
- 删掉 `out/production/` 后干净通过：旧路径是 IntelliJ/Maven 历史产物，不再需要

### 工具小坑

- background task 的 stdout/stderr 都被 Bash 截掉时，重定向到 `/tmp/xxx.log` 然后 `cat` 读取；`run_in_background=true` 后立刻去读大概率是空的（任务还没结束）
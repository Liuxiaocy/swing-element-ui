# P4-D/E/F 实现计划（spec: 2026-08-29-p4-def-design.md）

**Architecture:** D 批先把 AstIcon 图标绘制抽成静态 `paintIcon`（枚举 Type），组件实例只是薄包装；E 批 AstCard 加 Shadow 枚举三态；F 批按组件逐个小改，每项复用 P3 的 R1 尺寸配方。全部 Java 8 原生。

## Task D：AstIcon 重构
- [ ] D1 枚举 `Type`（40+ 个），旧 int 常量值=序号，旧构造器委托。
- [ ] D2 `paintIcon(Graphics2D, Type, Color, int size, float spin)` 静态绘制器；
      图标绘制全部改为 24px 逻辑坐标 → `scale = size/24f` 变换，网格统一。
- [ ] D3 新图标绘制：CALENDAR/CLOCK/STAR/STAR_FILLED/BELL/MESSAGE/MORE/MENU/LINK/
      LOCATION/PHONE/CAMERA/COLLECTION/UPLOAD/DOWNLOAD/LOCK/UNLOCK/SORT/FILTER_FILLED/
      FULL_SCREEN/COPY/SHARE/PRINT/CIRCLE_CHECK/CIRCLE_CLOSE/CIRCLE_WARNING/CIRCLE_INFO/
      QUESTION/LOADING/CARET_UP/DOWN/LEFT/RIGHT（34 新 + 原 20 = 54 图标）。
- [ ] D4 LOADING 自旋：`setSpinEnabled(true)` 用 Timer 每 40ms 转相位 repaint。
- [ ] D5 selfCheck：① 每 Type 绘制落笔（非全透明像素>0）② 旧常量==枚举序号
      ③ 反向验证：注释掉一个新图标绘制体 → 断言 exit=1。
- [ ] D6 新建 `AstIconDemo`（图标网格 + 尺寸/色变体 + 旋转段 + selfcheck 离屏绘制全图标）。
- [ ] D7 build.bat：SOURCES 增 `AstIconDemo.java`，自检链末尾追加；提交
      `feat(P4-d): AstIcon 重构为枚举 API + 图标库 54 个 + 静态绘制器 + 旋转动画`。

## Task E：AstCard 阴影三态 + 模板
- [ ] E1 `enum Shadow{ALWAYS,HOVER,NEVER}` + `setShadow`；旧构造器映射（true→HOVER,false→NEVER）。
      绘制：ALWAYS 恒定阴影 alpha；HOVER 由 hover 动画插值；NEVER 无。
- [ ] E2 无头卡片：title 空时不画 header（断言高度 < 有头）。
- [ ] E3 selfCheck：三态阴影 alpha 断言（HOVER 走 hoverAnim）、兼容映射断言、反向验证。
- [ ] E4 `AstCardDemo` 扩展：图片卡片/列表卡片/统计卡片/三态对比/带操作卡片。
- [ ] E5 提交 `feat(P4-e): AstCard 阴影三态 + 无头卡片 + 5 类卡片模板 demo`。

## Task F：精修（4 小提交）
- [ ] F1 档位：AstPagination/AstRate/AstForm/AstBadge 套 R1 + 断言 + 反向验证；
      提交 `feat(P4-f1): 尺寸档位补全(Pagination/Rate/Form/Badge)`。
- [ ] F2 Badge：max 溢出 `max+`、hidden、Type 5 色配色 + WCAG 断言；
      提交 `feat(P4-f2): AstBadge max 溢出/hidden/type 配色`。
- [ ] F3 Loading：setBgColor/setSpinnerSize/setDelay(ms) + 断言；
      提交 `feat(P4-f3): AstLoading 背景色/环尺寸/延迟显示`。
- [ ] F4 Transfer：setTitles/setButtonTexts/空态「无数据」+ 断言；
      提交 `feat(P4-f4): AstTransfer 标题/按钮文案/空态`。

## 验证（每批后）
1. 原生 JDK 8 全量编译（`C:/Program Files/Java/jdk1.8.0_311/bin/javac.exe`）。
2. 新断言反向验证（回退实现 → exit=1）。
3. 全量 self-check（34→36 项，新增 AstIcon/AstIconDemo）ALL PASS。
4. 独立提交（中文 message）。
